[project]
name = "alenax"
version = "0.1.2"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = ["gymnax>=0.0.9", "ale-py"]

[project.optional-dependencies]
dev = [
    "mypy>=1.19.1",
    "pytest>=8",
    "black",
    "isort",
]
